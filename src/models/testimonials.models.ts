export interface CardReviewsProps {
  style?: React.CSSProperties;
  comments: string;
  className?: string;
}

export interface CardTopUpProps {
  style?: React.CSSProperties;
  className?: string;
  transfer: boolean;
  headerSection: string;
  bodySection: string;
  src: any;
  alt: string;
}
